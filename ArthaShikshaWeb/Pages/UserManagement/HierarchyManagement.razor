@page "/hierarchy-management"


<div class="hierarchy-management-bg">
    <div class="hierarchy-management-center">
        <div class="hierarchy-empty-card">
            <img src="images/empty-hierarchy.svg" alt="Hierarchy Coming Soon" class="hierarchy-empty-img" />
            <div class="hierarchy-empty-title">Hierarchy Management Coming Soon!</div>
            <div class="hierarchy-empty-desc">
                If you want to check in the development process, schedule a demo meet or reach out to get acknowledgement.
            </div>
            <div class="hierarchy-empty-actions">
                <button class="btn btn-primary" @onclick="ScheduleMeeting">
                    <span class="material-icons">event</span>
                    Schedule Meeting
                </button>
            </div>
        </div>
    </div>
</div>

@code {
    private void ScheduleMeeting()
    {
        // Implement scheduling logic or link here
    }
}

<style>
.hierarchy-management-bg {
    min-height: 100vh;
    width: 100%;
    background: linear-gradient(135deg, #f5f7fa 0%, #e8ecf1 100%);
    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: flex-start;
}

.hierarchy-management-center {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding: 32px 0 0 0;
}

.hierarchy-empty-card {
    width: 100%;
    max-width: 1700px;
    min-width: 600px;
    min-height: 350px;
    background: #fff;
    border: 1.5px solid #dbeafe;
    border-radius: 10px;
    margin: 32px auto 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 40px 0 40px 0;
    box-shadow: 0 2px 8px rgba(79, 125, 243, 0.04);
}

.hierarchy-empty-img {
    width: 180px;
    margin-bottom: 18px;
}

.hierarchy-empty-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #22223b;
    margin-bottom: 8px;
    text-align: center;
    margin-top: 8px;
}

.hierarchy-empty-desc {
    font-size: 1rem;
    color: #8a8a99;
    margin-bottom: 28px;
    text-align: center;
    max-width: 600px;
}

.hierarchy-empty-actions {
    display: flex;
    gap: 16px;
    margin-top: 8px;
}

.btn {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 1rem;
    font-weight: 500;
    border-radius: 6px;
    padding: 8px 20px;
    border: none;
    cursor: pointer;
    transition: background 0.2s, color 0.2s, border 0.2s;
    outline: none;
}

.btn-primary {
    background: #4f7df3;
    color: #fff;
    border: 1.5px solid #4f7df3;
}

.btn-primary:hover {
    background: #3b5fc9;
    border-color: #3b5fc9;
}

.material-icons {
    font-size: 20px;
    vertical-align: middle;
}
</style>
